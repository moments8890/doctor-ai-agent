# Models Module

Purpose:
- Pydantic/domain models used by service and router boundaries.

Key files:
- `medical_record.py`: structured medical record payload model.
- `neuro_case.py`: neuro specialty structured case model.

Notes:
- DB ORM models are in `db/models.py` (not here).
