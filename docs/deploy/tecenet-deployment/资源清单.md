# 腾讯云资源清单与现状（doctor-ai）

> 截至 2026-04-25 自动汇总。修改资源后请重新生成或手工更新本文件。

## 1. 一句话概览

| 资源 | 用途 |
|---|---|
| **CVM `doctor-ai-prod-cvm-01`** | 唯一一台生产服务器，跑后端、前端、webhook、监控栈 |
| **DNSPod `doctoragentai.cn`** | 域名解析（含邮件相关 SPF/DKIM/DMARC） |
| **SES `doctoragentai.cn`** | 邮件推送，用于发送系统告警邮件 |
| **COS `doctor-ai-backups-1408751198`** | 对象存储，存放 MySQL 每日备份（90 天自动过期） |
| **云监控 `policy-i47hxgq5`** | CVM 资源告警（CPU/内存/磁盘/网络/事件），免费 |
| **CAM 子用户 `claude-automation`** | 程序化访问腾讯云 API 的专用身份，限定权限 |

---

## 2. 域名与 DNS（DNSPod）

域名：**`doctoragentai.cn`**，托管在 DNSPod 免费版，主名称服务器 `clark.dnspod.net` / `optional.dnspod.net`。

| 主机记录 | 类型 | 记录值 | 用途 |
|---|---|---|---|
| `@` | NS | `clark.dnspod.net.` | DNSPod 默认 NS（不可修改） |
| `@` | NS | `optional.dnspod.net.` | DNSPod 默认 NS（不可修改） |
| `api` | A | `101.35.116.122` | `api.doctoragentai.cn` 指向生产 CVM，承载后端 API、前端、webhook、GlitchTip |
| `@` | MX (优先级 5) | `mxbiz1.qq.com.` | 收信指向腾讯企业邮（目前没有真实收件箱，仅占位） |
| `@` | TXT | `v=spf1 include:qcloudmail.com ~all` | SPF — 授权腾讯 SES 代发 |
| `qcloud._domainkey` | TXT | `v=DKIM1; k=rsa; p=...` | DKIM 公钥 — 邮件签名校验 |
| `_dmarc` | TXT | `v=DMARC1; p=none` | DMARC 监控策略（仅监测，不强制） |

**修改 DNS：** https://console.dnspod.cn/ → 我的域名 → `doctoragentai.cn` → 解析。

---

## 3. 云服务器（CVM）

| 字段 | 值 |
|---|---|
| 实例名 | `doctor-ai-prod-cvm-01` |
| 实例 ID | `ins-ijnd8p4d` |
| 区域 | **ap-shanghai**（华东上海） |
| 规格 | `SA9.MEDIUM8`（2 核 / 8 GB） |
| 系统盘 | 50 GB（**当前使用 86%，已接近警戒线**） |
| 公网 IP | `101.35.116.122` |
| 内网 IP | `10.10.12.7` |
| 操作系统 | Ubuntu 22.04 LTS 64-bit |
| 状态 | RUNNING |
| 创建时间 | 2026-03-06 |
| 到期时间 | 2026-06-13（**注意续费**） |
| SSH 别名 | `tencent`（密钥 `~/.ssh/tencent/doctor-ai-prod-cvm-01.pem`，用户 `ubuntu`） |

**纳管：** `ssh tencent` 进入；`tccli cvm DescribeInstances --region ap-shanghai` 查看程序化信息。

---

## 4. 邮件推送（SES，香港区域）

> ⚠️ SES 资源在 **`ap-hongkong`** 区域，使用 tccli 调用必须显式 `--region ap-hongkong`。

### 4.1 发信域名

| 字段 | 值 |
|---|---|
| 域名 | `doctoragentai.cn` |
| 状态 | 验证通过（4 条 DNS 记录已配置，见上） |
| 信誉等级 | 1 / 1（最高） |
| 单日配额 | 500 封 |

### 4.2 发信地址

| 邮箱 | 显示名 | SMTP 密码 | 用途 |
|---|---|---|---|
| `alerts@doctoragentai.cn` | Deploy Alerts | 已设置 | 部署漂移、后端异常等系统告警 |

**SMTP 接入信息：**
- 服务器：`smtp.qcloudmail.com`
- 端口：`465`（SSL）或 `587`（STARTTLS）
- 用户名：`alerts@doctoragentai.cn`（即发信地址本身）
- 密码：通过 `tccli ses UpdateEmailSmtpPassWord` 设置，不可读取，只能重置

### 4.3 邮件模板

| 模板名 | TemplateID | 状态 | 用途 |
|---|---|---|---|
| `doctor_ai_system_alert` | **174869** | **审核中** ⏳ | 系统告警通用模板（变量：`alert_title`/`alert_summary`/`alert_detail`/`alert_time`/`alert_host`/`alert_action`） |

模板 HTML 源文件：`deploy/tencent/ses_template_system_alert.html`

> **TemplateStatus 含义**：`0` = 审核通过 ✅ ; `1` = 审核中 ⏳ ; `2` = 审核未通过 ❌（`ReviewReason` 字段会说明原因）。
> 当前 174869 状态为 1，预计 1 个工作日内出结果。审核通过后可立即用于 `tccli ses SendEmail --Template` 调用。

### 4.4 自定义发信权限（待申请）

⚠️ **目前账号尚未开通"自定义发信权限"**，因此：
- ✅ 通过 **API + 模板** 发送（drift_check.sh 可用此路径）→ 工作正常
- ❌ 通过 **SMTP 任意正文** 发送（GlitchTip 想自己写邮件）→ 被服务端拦截，返回 `FailedOperation.WithOutPermission 必须使用模板发送`

后续如要让 GlitchTip 自动发送后端异常告警邮件，需在 SES 控制台手动申请"自定义发信权限"，1–3 个工作日审批。

---

## 5. 主机上运行的服务

### 5.1 systemd 服务

| 服务 | 状态 | 用途 |
|---|---|---|
| `doctor-ai-backend.service` | active | FastAPI 后端（uvicorn），监听 127.0.0.1:8000 |
| `doctor-ai-webhook.service` | active | Gitee push 钩子接收器，监听 0.0.0.0:9000，触发 `deploy.sh` |
| `nginx.service` | active | 反向代理：443 → 8000（后端）、9000（webhook）、8100（GlitchTip 子路径） |
| ~~cloudflared-doctor.service~~ | inactive | 历史遗留，未使用，建议清理 |

### 5.2 nginx 站点

- ✅ `/etc/nginx/sites-enabled/doctoragentai.cn` — 当前生效
- ⚠️ `doctoragentai.cn.bak-20260423-023136` — 备份残留，建议归档到 `sites-available/archive/`
- ⚠️ `doctoragentai.cn.bak-20260423-023211` — 同上

**TLS 证书：** Let's Encrypt 签发，`api.doctoragentai.cn`，到期 **2026-06-06**（certbot 自动续期）。

### 5.3 Docker 容器

| 容器 | 镜像 | 端口 | 用途 |
|---|---|---|---|
| `glitchtip-web-1` | `glitchtip:latest` | 127.0.0.1:8100→8080 | 自托管 Sentry 兼容错误追踪（Web UI + 事件入口） |
| `glitchtip-worker-1` | `glitchtip:latest` | — | GlitchTip 后台 worker（celery + beat） |
| `glitchtip-postgres-1` | `postgres:16-alpine` | 5432/tcp（仅容器内） | GlitchTip 数据库 |
| `glitchtip-redis-1` | `redis:7-alpine` | 6379/tcp（仅容器内） | GlitchTip 队列 |
| `dbgate-adminer-1` | `adminer:4` | 127.0.0.1:8101 | 业务数据库 Web 管理工具 |
| `n8n` | `n8n:latest` | — | 工作流自动化（用途待确认） |
| `doctor-ai-mysql` | `mysql:8.0` | 127.0.0.1:3306 | 业务数据库（生产 MySQL） |

### 5.4 计划任务（ubuntu 用户 crontab）

| 频率 | 脚本 | 用途 |
|---|---|---|
| 每 5 分钟 | `auto_deploy_on_drift.sh` | **部署自愈** — 拉取 gitee/main，若与本地 HEAD 不一致且提交时间 ≥5 分钟，自动跑 deploy.sh。容错丢失的 webhook |
| 每天 03:17 | `mysql_backup.py` | **MySQL→COS 备份** — 拉取容器内 mysqldump，gzip，上传到 cos://doctor-ai-backups-1408751198/mysql-daily/，COS 90 天自动过期 |
| 每天 04:15 | `backup_mysql.sh` | **MySQL→GPG→Mac mini 备份** — 加密版本，本地保留 14 天，Mac mini 拉走长期保管。私钥只在 Mac mini 上 |
| 每天 09:07 | `drift_check.sh` | **部署告警** — 比较 gitee 与本地 HEAD，写 GlitchTip 事件 |
| 每周一 09:13 | `weekly_health_check.sh` | **基础设施巡检** — 磁盘/证书/服务/CVM 到期/SES 模板状态 |

**备份策略说明 — 两条并行链路（防御纵深）：**
1. `mysql_backup.py` → COS：plaintext，自动化，立即异地容灾，最快可恢复
2. `backup_mysql.sh` → 本地 GPG 加密 → Mac mini pull：加密，离线归档，私钥与生产隔离

任一链路独立可恢复全量数据。

---

## 6. 云监控告警（免费）

### 告警策略 `policy-i47hxgq5` — doctor-ai-prod CVM 主机告警

绑定到实例 `ins-ijnd8p4d`，使用通知模板 `notice-cxqw2bzb`（系统预设，发往主账号邮箱+短信）。

| 指标 | 阈值 | 持续 | 重发频率 |
|---|---|---|---|
| `CvmDiskUsage` 磁盘利用率 | > 85% | 5 分钟 | 每 1 小时 |
| `MemUsage` 内存利用率 | > 85% | 5 分钟 | 每 1 小时 |
| `CpuUsage` CPU 利用率 | > 80% | 5 分钟 | 每 1 小时 |
| `Outratio` 外网出带宽利用率 | > 85% | 5 分钟 | 每 1 小时 |

**事件告警**（无阈值，事件触发即告警）：
- `ping_unreachable` — 主机不可达
- `guest_reboot` — 系统重启
- `disk_readonly` — 磁盘只读

> **修改通知收件人**：腾讯云监控通知发往主账号绑定邮箱。如需添加其他收件人：
> 控制台 → 账号信息 → 安全设置 → 添加邮箱 jingyusuixing@outlook.com 并验证。
> 或在云监控 → 告警管理 → 通知模板 创建新模板挂到此策略上。

### 默认策略 `policy-u79cm383`

腾讯云为新账号自动创建的"默认"策略，阈值 95%，未绑定任何实例（`InstanceSum: 0`）。
保留作为兜底，不会重复告警。

---

## 7. 自动化身份（CAM）

### 6.1 子用户 `claude-automation`

| 字段 | 值 |
|---|---|
| 用户名 | `claude-automation` |
| Uin | `100048402079` |
| 主账号 OwnerUin | `100046697089` |
| AppId | `1408751198` |
| 访问方式 | 仅编程访问（无控制台登录） |

### 6.2 已挂策略（最小权限）

| 策略 | 覆盖 |
|---|---|
| `QcloudSESFullAccess` | 邮件推送：发信域名/地址/模板，SMTP 密码管理，配额 |
| `QcloudDNSPodFullAccess` | DNSPod：域名记录管理 |
| `QcloudCVMFullAccess` | 云服务器：实例生命周期、快照、安全组 |
| `QcloudCOSFullAccess` | 对象存储：备份、日志归档 |
| `QcloudCDNFullAccess` | CDN：缓存清理、域名配置 |
| `QcloudMonitorFullAccess` | 云监控：告警规则 |
| `QcloudCamReadOnlyAccess` | CAM 只读（用于排错时查询自身权限，**禁止提权**） |

**显式未授权（重要）：** 完整 CAM、计费财务、身份管理 — 即使密钥泄漏也无法提权或动账单。

### 6.3 凭据存放位置

- **本地（jingwu 的 Mac）：** `~/.tccli/credential` （chmod 600，**不进 git**）
- **本地 tccli 配置：** `~/.tccli/configure`（默认 region=ap-guangzhou）
- **CLI 路径：** `/Users/jingwuxu/.local/bin/tccli`（pipx 安装）

### 6.4 凭据轮换

- CAM 允许同账号同时存在 2 个有效密钥 → 平滑轮换
- 建议每 90 天轮换一次
- 轮换流程：新建 → 替换 `~/.tccli/credential` → 旧密钥**禁用**（不删）24h → 删除

---

## 8. 监控告警链路

```
┌─────────────────┐  every 5 min  ┌──────────────────┐
│ auto_deploy_    │──────────────▶│ deploy.sh        │
│   on_drift.sh   │  (self-heal)  │ (auto pull+build) │
└─────────────────┘               └──────────────────┘
        │
        │ 兜底
        ▼
┌─────────────────┐  push event   ┌──────────────────┐
│ Gitee webhook   │──────────────▶│ doctor-ai-       │
│ (主路径)         │  port 9000    │   webhook svc    │
└─────────────────┘               └────────┬─────────┘
                                           │
                                           ▼
                                  ┌──────────────────┐
                                  │ deploy.sh        │
                                  │ git fetch+reset  │
                                  │ frontend build   │
                                  │ backend restart  │
                                  └──────────────────┘

┌─────────────────┐  daily 09:07  ┌──────────────────┐
│ drift_check.sh  │──────────────▶│ GlitchTip event  │
└─────────────────┘  (alarm only) │ tag=deploy_drift │
                                  └────────┬─────────┘
                                           │ ⚠️ 邮件路径待打通
                                           ▼
                                  ┌──────────────────┐
                                  │ Tencent SES      │  →  jingyusuixing@outlook.com
                                  │ template 174869  │
                                  └──────────────────┘
```

**当前邮件链路状态：**
- ⏳ 模板审核中（TemplateID 174869，状态 1）
- ⏸️ `drift_check.sh` 重写走模板 API 的工作待模板审核通过后启动
- ⏳ 待申请"自定义发信权限"以让 GlitchTip 自身的告警邮件也能发出（用于后端异常）

---

## 9. 待办事项

| 优先级 | 事项 | 备注 |
|---|---|---|
| 🔴 高 | CVM 系统盘清理（86% 已用） | 排查 `/var/log`、Docker 镜像、旧日志；考虑加挂数据盘 |
| 🔴 高 | 重写 `drift_check.sh` 走模板 API | **阻塞中** — 等待模板 174869 审批通过 |
| 🟡 中 | 申请 SES "自定义发信权限" | 解锁 GlitchTip→SMTP 路径，1–3 工作日审批 |
| 🟡 中 | CVM 续费提醒（2026-06-13 到期） | 加日历或申请自动续费 |
| 🟢 低 | 清理 nginx 备份站点 `*.bak-*` | 移到 `sites-available/archive/` |
| 🟢 低 | 删除或修复 `cloudflared-doctor.service` | 已 dead 1 个多月，确认是否还需要 |
| 🟢 低 | DKIM 公钥按 12 个月节奏轮换 | 当前是初始密钥 |
| 🟢 低 | DMARC 策略升级 `p=none → p=quarantine` | 监控 ~2 周后切换 |

---

## 10. 关键文件与命令速查

```bash
# SSH 进生产
ssh tencent

# 查看部署历史
ssh tencent 'tail -50 /home/ubuntu/doctor-ai-agent/logs/deploy.log'

# 查看告警检查日志
ssh tencent 'tail -50 /home/ubuntu/doctor-ai-agent/logs/drift_check.log'
ssh tencent 'tail -50 /home/ubuntu/doctor-ai-agent/logs/auto_deploy.log'

# webhook 状态
ssh tencent 'sudo journalctl -u doctor-ai-webhook.service -n 50 --no-pager'

# 后端健康
curl https://api.doctoragentai.cn/healthz

# GlitchTip UI（需 SSH 隧道）
ssh -L 8100:127.0.0.1:8100 tencent  # 然后浏览器开 http://localhost:8100

# 手动触发部署
ssh tencent 'bash /home/ubuntu/doctor-ai-agent/deploy/tencent/deploy.sh'

# 程序化访问腾讯云
tccli ses ListEmailIdentities --region ap-hongkong
tccli cvm DescribeInstances --region ap-shanghai
tccli dnspod DescribeRecordList --Domain doctoragentai.cn
```
