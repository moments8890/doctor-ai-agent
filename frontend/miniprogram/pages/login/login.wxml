<!-- Auto-login check loading state -->
<view wx:if="{{loading}}" class="loading-overlay">
  <view class="loading-spinner" />
  <text class="loading-hint">正在登录...</text>
</view>

<!-- Web login page -->
<web-view
  wx:if="{{url}}"
  src="{{url}}"
  bindmessage="onMessage"
/>
