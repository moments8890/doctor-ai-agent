# Copy to .env.ios (gitignored) and fill in your production server URL.
# This is required for the Capacitor/iOS build — relative /api/... paths
# don't resolve inside the iOS WKWebView.
VITE_API_BASE_URL=https://your-server.com
