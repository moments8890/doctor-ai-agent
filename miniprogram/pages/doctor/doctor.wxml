<view wx:if="{{loading && url}}" class="loading" bindtap="onLoadingTap">
  <view class="loading-spinner" />
  <text class="loading-text">加载中…</text>
</view>
<web-view wx:if="{{url}}" src="{{url}}" bindmessage="onMessage" bindload="onWebViewLoad" />
