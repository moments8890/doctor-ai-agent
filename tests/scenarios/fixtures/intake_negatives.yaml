id: intake-negatives-001
title: "Intake: negative values count as collected"
tags: [intake, extraction]

setup:
  patients:
    - ref: p1
      name: 李强
      gender: 男
      age: 40

steps:
  - id: start
    call: doctor_intake.start
    input:
      patient_id: ${patients.p1}
      mode: doctor
      initial_fields:
        chief_complaint: "头痛三天"
    expect:
      hard:
        session_id.exists: true
        status: active

  - id: negatives
    call: doctor_intake.turn
    input:
      session_id: ${steps.start.session_id}
      text: "无特殊既往史，无药物过敏，无家族遗传病史，不吸烟不饮酒"
    expect:
      hard:
        collected.past_history.contains_any: ["无"]
        collected.allergy_history.contains_any: ["无"]
        collected.family_history.contains_any: ["无"]
        collected.personal_history.contains_any: ["不吸烟", "不饮酒"]
