[pytest]
asyncio_mode = auto
testpaths = tests
addopts = --strict-markers -ra
markers =
    integration: end-to-end tests requiring a running server (port 8001) and LAN Ollama
    slow: on-demand tests that call real LLM providers (diagnosis sniff, etc.); require a PROVIDER env and API key
